<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{title}}</title>
</head>
<body>
  <h1>{{title}}</h1>
  <p>{{dynamicContent}}</p>
  
  <h2>home List</h2>
  <ul>
    {{#each items}}
      <li>
        {{name}}: {{description}}
      </li>
    {{/each}}
  </ul>

  {{> component }}
</body>
</html>
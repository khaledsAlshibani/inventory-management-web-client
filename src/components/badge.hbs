<div class="badge badge--{{variant}} {{class}}">
    {{#if iconName}}
        <i class="badge__icon icon-{{iconName}}" aria-hidden="true"></i>
    {{/if}}
    <span class="badge__label">{{label}}</span>
</div>